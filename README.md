# SitStandTracker

A tiny native macOS app for tracking time spent sitting and standing.

## Features

- One-click switching between `Sitting` and `Standing`
- Live timer for the current posture
- Daily totals for both postures
- Session history for the current day
- Local persistence via `UserDefaults`

## Run

From the repo root:

```bash
cd mac/SitStandTracker
swift run
```

## Open In Xcode

```bash
cd mac/SitStandTracker
open Package.swift
```
